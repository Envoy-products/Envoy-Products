<div class="col-sm-4 col-xs-12">
    <div class="panel panel-default text-center">
        <div class="panel-heading">
            <h1>{{name}}</h1>
        </div>
        <div class="panel-body">
            <img src="{{product_img}}" alt="" width="150" height="150">
            <p>{{description}}</p>
            <hr>
            <a href="{{website}}"><p>{{website}}</p></a>
  
        </div>
    </div>
</div>