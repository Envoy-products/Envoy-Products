<div class="panel">
    <div class="panel-heading">
        <h1>{{name}}</h1>
    </div>
    <div class="panel-body">
        <img class="product-picture" src="{{product_img}}" alt="" width="150" height="150">
        <p class="product-text">{{description}}</p>
        <hr>
        <div class="text-center">
            <a class="btn-card decoration-none" class="product-text" href="/products/{{id}}">
                See Details
            </a>
        </div>
        <div class="panel-body">
            <img class="product-picture" src="{{product_img}}" alt="{{name}}" height="150">
                <p class="product-text">{{description}}</p>
            <hr>
            <div align="center">
                <a class="button" href="/products/{{id}}">Learn More</a>
            </div>
        </div>
    </div>
